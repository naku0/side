@import url('https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&display=swap');

:root {
    --black: #242425;
    --dark-grey: #333533;
    --text-white: #e8eddf;
    --background-white: #cfdbd5;
    --gold: #f5cb5c;
    --hover-text: #FFFFFF;
    --hover-black: #000000;
}

.App {
    display: flex;
    flex-direction: column;
    background-color: var(--black);
    color: var(--text-white);
}

header{
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1;
    background: linear-gradient(to bottom, var(--black) 50%, transparent 100%);
}
.video{
    position: relative;
    z-index: 0;
    top: 0;
}

.video img{
    position: relative;
    width: 100%;
    object-fit: cover;
}
