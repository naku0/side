.header {
    display: flex;
    flex-direction: row;
    list-style-type: none;
    place-content: center;
    font-size: 1.5em;
    background-color: transparent;
}

.header.scroll{
    background-color: var(--black);
}

li {
    place-content: center;
}

.header__logo img{
    width: 600px;
    padding: 20px;
    transition: width 0.5s ease-in-out;
}

.header__logo img.scroll{
    width: 300px;
}

button {
    font-size: 0.8em;
    background-color: transparent;
    border: none;
    text-decoration: none;
    color: var(--background-white);
    display: flex;
    place-content: center;
}

button:hover {
    color: var(--text-white);
    cursor: pointer;
}

button:active {
    color: var(--hover-text);
}

.header__menu {
    display: none;
    place-content: center;
    place-items: center;
    margin-right: 40px;
    padding: 0;
}

@media (max-width: 768px) {
    .header__menu {
        display: flex;
        margin-left: auto;
        width: min-content;
    }

    .header li:not(.header__logo, .header__menu) {
        display: none;
    }

    .header__logo img{
        width: 400px;
        padding: 20px;
        transition: width 0.5s ease-in-out;
    }

    .header__logo img.scroll{
        width: 200px;
    }

}

@media (hover: none) {
    button:focus-visible {
        color: var(--hover-text);
    }
}
